# Copy to .env.local (git-ignored) to override. If unset, the app calls the API
# through the Vite dev proxy at /api (see vite.config.js) — no CORS setup needed.
#
# Point straight at the backend instead (e.g. when serving the built bundle):
# VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
